package com.bank.demo.controller.mapper;
import com.bank.demo.controller.response.AccountDTO;
import com.bank.demo.entity.Account;

public interface AccountMapper {
	AccountDTO accountDTOtoAccount(Account acc);
	Account accounttoAccountDTO(AccountDTO accResponse);
}
